{
  "buildCommand": "prisma generate && next build",
  "regions": ["iad1"],
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 60
    },
    "app/actions/**/*.ts": {
      "maxDuration": 60
    }
  }
}
